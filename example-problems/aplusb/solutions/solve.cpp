#include <iostream>
#include <cstdio>

int main() {
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);

    int a, b;
    std::cin >> a >> b;
    std::cout << a + b << std::endl;
    return 0;
}
